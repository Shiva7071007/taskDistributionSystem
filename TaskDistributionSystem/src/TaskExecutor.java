public class TaskExecutor {
    String result = null;

    // this method either keep running always or till all task are executed
	public static String executeTask(taskId, nodeIP) {
        //logic
        result = "result";    
		return result;
    }

    public void storeResult(){
        // store result into db
    }
}